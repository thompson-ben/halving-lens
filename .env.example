# ---------------------------------------------------------------------------
# Supercar Content Engine — environment variables
# Copy this file to `.env.local` and fill in real values for local development.
# Never commit real secrets.
# ---------------------------------------------------------------------------

# ---- Application -----------------------------------------------------------
NODE_ENV=development
NEXT_PUBLIC_APP_NAME="Supercar Content Engine"
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Toggle to fall back to mock data if external APIs are unavailable.
USE_MOCK_DATA=true

# ---- Database --------------------------------------------------------------
# PostgreSQL (Supabase / Neon / RDS / local). The Prisma schema uses String[]
# and Json columns, so Postgres is required as-is. To use SQLite locally,
# switch `provider` in prisma/schema.prisma and replace String[] / Json
# columns with their SQLite-compatible equivalents.
#
# On Supabase, use the pooled connection here (port 6543, PgBouncer) for
# serverless runtime queries, and DIRECT_URL below for migrations.
DATABASE_URL="postgresql://user:password@localhost:5432/supercar_engine?schema=public"
# Direct (non-pooled) Postgres URL used by Prisma for migrations and
# introspection. On Supabase this is the port-5432 connection string. For
# local Postgres you can leave it equal to DATABASE_URL.
DIRECT_URL="postgresql://user:password@localhost:5432/supercar_engine?schema=public"

# Supabase (optional; only required if using Supabase auth / storage)
SUPABASE_URL=
SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# ---- OpenAI ----------------------------------------------------------------
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini

# ---- Meta / Instagram Graph API --------------------------------------------
# See META_API_SETUP.md for the full guided walkthrough.
META_APP_ID=
META_APP_SECRET=
META_REDIRECT_URI=http://localhost:3000/api/instagram/callback
# Long-lived user access token (server-side only). Rotate regularly.
INSTAGRAM_ACCESS_TOKEN=
# Instagram Business Account ID (numeric).
INSTAGRAM_BUSINESS_ACCOUNT_ID=
# Facebook Page ID that the IG Business Account is connected to.
FACEBOOK_PAGE_ID=

# ---- Optional: queue / scheduling ------------------------------------------
REDIS_URL=
# Secret for verifying cron jobs that trigger /api/cron/*
CRON_SECRET=

# ---- Optional source connectors --------------------------------------------
<<<<<<< HEAD
# TikTok Display API. NOTE: the Display API only exposes the
# *authenticated user's own* videos (scopes: user.info.basic, video.list),
# not arbitrary discovery. Drop a long-lived TIKTOK_ACCESS_TOKEN here to
# sync your own account's TikTok videos into the discovery queue.
=======
# RSS / Atom — no credentials required. Feed URLs are stored on the Source
# row in the database (Source.config.feeds) and editable from the seed
# script or future Settings UI.
#
# TikTok (Display API)
>>>>>>> origin/main
TIKTOK_CLIENT_KEY=
TIKTOK_CLIENT_SECRET=
TIKTOK_ACCESS_TOKEN=

# YouTube Data API v3 — server-side key with the Data API v3 enabled.
# Quota: 10,000 units/day on the free tier. Search costs 100/call, so the
# default 5-query daily sync uses ~550 units.
YOUTUBE_API_KEY=

# X / Twitter API v2 (Bearer token)
TWITTER_BEARER_TOKEN=
